package suffixTreeClustering.svd;

import java.util.ArrayList;
import java.util.List;
import java.util.Set;

import suffixTreeClustering.data.Type;

public class DimensionReducer {

	public static List<Type> reduce(Set<Type> types) {
		List<Type> toReturn = new ArrayList<Type>(types);

		/*
		 * TODOs:
		 * * create matrix from type vectors
		 * * give matrix zu svd algorithm
		 * * get back the term vectors
		 * * set the type's vectors
		 */
		
		return toReturn;
	}

}
